// Re-export depuis le point canonique — @/api/axiosConfig
// Ce fichier existe pour la compatibilité descendante des imports existants.
export { default, setAuthToken, clearAuthToken } from "@/api/axiosConfig"
